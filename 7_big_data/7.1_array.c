#include <stdio.h>
int main()
{
    // definition
    int num_array[3];
    double price_array[3];

    // allocate first array
    num_array[0] = 2;
    num_array[1] = 7;
    num_array[2] = 3;

    // allocate second array
    price_array[0] = 1.45;
    price_array[1] = 0.85;
    price_array[2] = 0.75;

    return 0;
}